<!-- GENERATED by `waystation` — do not edit by hand. Edits are overwritten. -->

# task\-lock\-contended\-wiring — Wire lock contention to the lock\_contended code
status: todo  priority: 2  scope: scope-core
readiness: not_eligible  reason: status_todo

From the 2026\-08\-15 complete code audit: the code catalog defines \`lock\_contended\` \(src/core/result\.ts:222, retryable: true\) but nothing ever emits it\. When \`proper\-lockfile\.lock\` in \`withLedgerLock\` \(src/core/store\.ts:306\-310\) exhausts its retries it throws a raw Error, which surfaces as \`unexpected\_error\` \(retryable: false\) in CLI/MCP/dashboard\. The intended retry guidance is dead\. Fix: catch the lock\-acquisition failure in \`withLedgerLock\` \(or the surface error mappers\) and emit \`lock\_contended\`\.

## Acceptance
- A lock\-contention timeout surfaces as a diagnostic with code \`lock\_contended\` and \`retryable: true\` in all three surfaces \(CLI, MCP, dashboard\), not \`unexpected\_error\`\.
- The fix does not mask genuine unexpected errors\.
- A test simulates lock contention \(held lock \+ short/forced failure\) and asserts the \`lock\_contended\` code\.
- All checks green: \`bun test\`, \`bun run typecheck\`, \`bun run check\`, \`waystation validate\`\.
